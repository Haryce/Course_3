<!-- [file name]: views/ItemsWindow.xaml -->
<Window x:Class="StoreG5G11.views.ItemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Управление товарами" Height="500" Width="900">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Список товаров" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <!-- Фильтр -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Категория:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Name="filterComboBox" Width="150" 
                      SelectionChanged="FilterByCategory_SelectionChanged">
                <ComboBoxItem Content="Все товары" Tag=""/>
                <ComboBoxItem Content="Кофе" Tag="Coffee"/>
                <ComboBoxItem Content="Выпечка" Tag="Bakery"/>
            </ComboBox>
        </StackPanel>
        
        <!-- Кнопки управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="100" Margin="5" Click="AddProduct_Click"/>
            <Button Content="Редактировать" Width="100" Margin="5" Click="EditProduct_Click"/>
            <Button Content="Удалить" Width="100" Margin="5" Click="DeleteProduct_Click"/>
        </StackPanel>
        
        <!-- Список товаров -->
        <DataGrid Grid.Row="2" Name="productsGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="250"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="100"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=N2}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Кнопка закрытия -->
        <Button Grid.Row="3" Content="Закрыть" Width="100" Margin="5" 
                HorizontalAlignment="Right" Click="Close_Click"/>
    </Grid>
</Window>