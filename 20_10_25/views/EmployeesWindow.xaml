<!-- [file name]: views/EmployeesWindow.xaml -->
<Window x:Class="StoreG5G11.views.EmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Управление сотрудниками" Height="450" Width="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Список сотрудников" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <!-- Кнопки управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="100" Margin="5" Click="AddEmployee_Click"/>
            <Button Content="Редактировать" Width="100" Margin="5" Click="EditEmployee_Click"/>
            <Button Content="Удалить" Width="100" Margin="5" Click="DeleteEmployee_Click"/>
        </StackPanel>
        
        <!-- Список сотрудников -->
        <DataGrid Grid.Row="1" Name="employeesGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="120"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Зарплата" Binding="{Binding Salary, StringFormat=N2}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Кнопка закрытия -->
        <Button Grid.Row="2" Content="Закрыть" Width="100" Margin="5" 
                HorizontalAlignment="Right" Click="Close_Click"/>
    </Grid>
</Window>