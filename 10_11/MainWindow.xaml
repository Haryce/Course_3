<Window x:Class="TwoWindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Main Window" Height="300" Width="400">
    <StackPanel Margin="20">
        <TextBlock Text="Главное окно" FontSize="16" Margin="0,0,0,20"/>
        <TextBox x:Name="InputTextBox" 
                 Text="{Binding SharedText, UpdateSourceTrigger=PropertyChanged}"
                 Height="30" Margin="0,0,0,10"/>
        <Button Content="Открыть второе окно" 
                Click="OpenSecondWindow_Click" 
                Height="30"/>
    </StackPanel>
</Window>